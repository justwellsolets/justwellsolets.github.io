import { UnsubscribeDirective } from './unsubscribe.directive';

describe('UnsubscribeDirective', () => {
  it('should create an instance', () => {
    const directive = new UnsubscribeDirective();
    expect(directive).toBeTruthy();
  });
});
